das modell des schießens und das schadensmodell könnten eigene klassen sein, um das erben von mehreren klassen zu demonstrieren
zwar wären sowohl schiffe als auch türme dann game objects und würden von den modellen erben, das könnte aber begründet sein, weil 
das schadensmodell semantisch nicht nach oben über die s/t passt. damit kann man dann zeigen, dass modularer code kein eigenes teil 
im physischen und semantischen sinne sein muss, sondern so eine kombination von eigenschaften möglich wird. 
============================================================================================================================
Erklären warum occupied space kein setter / updater braucht.

auf mro bei klassen eingehen, die von mehreren erben 

Ausrichtungsproblematik der Schiffe o

´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´---´´
In der Einleitung wird gesagt, es soll der Nutzen von Vererbung und Methodenüberschreibung  zur Spezialisierung von Klassen aufgezeigt werden. Das müsste noch passieren.
